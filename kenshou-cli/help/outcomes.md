# Outcomes

Runs finish as `passed`, `failed`, `errored`, `inconclusive`, or `infrastructure-failure`. A known defect never changes the true outcome; it can only make an explained failure non-blocking for automation.
