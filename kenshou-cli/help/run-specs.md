# Run specifications

A run specification is a versioned JSON request to execute one scenario. The effective document records every default explicitly, including dimensions, knobs, seed, phases, environment, and expected runtime cohort.
