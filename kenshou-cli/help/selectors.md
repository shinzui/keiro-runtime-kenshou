# Selectors

Selectors match scenario identifiers one segment at a time. `*` matches one segment and a final `**` matches the remainder. For example, `selftest/**` selects every self-test and `*/*/benchmark/*` selects all benchmark scenarios.
